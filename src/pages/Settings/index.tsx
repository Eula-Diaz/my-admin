import { Card, Typography } from "antd";

const { Title, Text } = Typography;

function Settings() {
  return (
    <Card>
      <Title level={2}>系统设置</Title>

      <Text>这里以后会实现系统设置。</Text>
    </Card>
  );
}

export default Settings;
